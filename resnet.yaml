model_path: "../../resnet/model/Net_0"
num_executor: 3
num_task: 150
devices: ["lynxi"]

input_files: "../../resnet/data/tank"

inputs:
  - shape: [1, 3, 224, 224]
    dtype: float32

outputs:
  - shape: [1,1000]
    dtype: float32